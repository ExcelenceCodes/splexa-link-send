import { createFileRoute, useNavigate } from "@tanstack/react-router";import { useState } from "react";import { AuthShell } from "./sign-in";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { supabase } from "@/integrations/supabase/client";
export const Route=createFileRoute("/reset-password")({head:()=>({meta:[{title:"Choose a new password — Splexanode"},{name:"description",content:"Set a new password for your Splexanode developer account."},{property:"og:title",content:"Choose a new Splexanode password"},{property:"og:description",content:"Complete account recovery securely."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Reset});
function Reset(){const[p,setP]=useState("");const[m,setM]=useState("");const nav=useNavigate();async function submit(e:React.FormEvent){e.preventDefault();if(p.length<12){setM("Use at least 12 characters.");return}const{error}=await supabase.auth.updateUser({password:p});if(error){setM(error.message);return}await supabase.auth.signOut({scope:"global"});void nav({to:"/sign-in"})}return <AuthShell title="Choose a new password" note="Use at least 12 characters. All existing sessions will be signed out."><form onSubmit={submit} className="mt-8 space-y-4"><Input className="h-11" type="password" value={p} onChange={e=>setP(e.target.value)} minLength={12} required/>{m&&<p className="text-sm text-destructive">{m}</p>}<Button className="h-11 w-full">Update password</Button></form></AuthShell>}
