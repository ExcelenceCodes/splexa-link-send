import { createFileRoute, Link } from "@tanstack/react-router";import { useState } from "react";import { AuthShell } from "./sign-in";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { supabase } from "@/integrations/supabase/client";
export const Route=createFileRoute("/sign-up")({head:()=>({meta:[{title:"Create a developer account — Splexanode"},{name:"description",content:"Create a verified Splexanode developer account."},{property:"og:title",content:"Create a Splexanode developer account"},{property:"og:description",content:"Build direct-to-cloud file transfer into your product."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:SignUp});
function SignUp(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[msg,setMsg]=useState("");async function submit(e:React.FormEvent){e.preventDefault();if(password.length<12){setMsg("Use at least 12 characters.");return}const{error}=await supabase.auth.signUp({email,password,options:{emailRedirectTo:`${window.location.origin}/verify-email`}});setMsg(error?error.message:"Check your email to verify your account.")}return <AuthShell title="Create your developer account" note="Your 5 GB allowance is shared across every app and widget."><form onSubmit={submit} className="mt-8 space-y-4"><label className="block"><span className="label">Work email</span><Input className="mt-2 h-11" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="block"><span className="label">Password</span><Input className="mt-2 h-11" type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={12}/></label>{msg&&<p className="text-sm text-muted-foreground">{msg}</p>}<Button className="h-11 w-full">Create account</Button></form><p className="mt-6 text-sm text-muted-foreground">Already registered? <Link to="/sign-in" className="font-medium text-primary">Sign in</Link></p></AuthShell>}
