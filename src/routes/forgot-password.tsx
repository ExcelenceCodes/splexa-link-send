import { createFileRoute } from "@tanstack/react-router";import { useState } from "react";import { AuthShell } from "./sign-in";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { supabase } from "@/integrations/supabase/client";
export const Route=createFileRoute("/forgot-password")({head:()=>({meta:[{title:"Reset password — Splexanode"},{name:"description",content:"Request a secure Splexanode password reset link."},{property:"og:title",content:"Reset your Splexanode password"},{property:"og:description",content:"Request a short-lived password reset link."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Forgot});
function Forgot(){const[email,setEmail]=useState("");const[done,setDone]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/reset-password`});setDone(true)}return <AuthShell title="Reset your password" note="We’ll send a short-lived reset link if the address is registered.">{done?<p className="mt-8 border-l-2 border-primary pl-4 text-sm">Check your inbox. The response is the same for every address.</p>:<form onSubmit={submit} className="mt-8 space-y-4"><label className="block"><span className="label">Email</span><Input className="mt-2 h-11" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><Button className="h-11 w-full">Send reset link</Button></form>}</AuthShell>}
